{
  "name": "callback-to-async-paysync",
  "version": "1.0.0",
  "description": "PaySync — callback-style payments client to migrate to async/await",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest --runInBand --testTimeout=30000",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/jest": "29.5.12",
    "@types/node": "20.12.7",
    "jest": "29.7.0",
    "ts-jest": "29.1.2",
    "typescript": "5.4.5"
  }
}
